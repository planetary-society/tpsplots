data:
  source: comparison_charts_controller.nasa_spending_as_part_of_annual_us_expenditures

chart:
  type: waffle
  output: nasa_spending_as_part_of_annual_us_expenditures
  title: "{{title}}"
  subtitle: "{{subtitle}}"
  source: "{{source}}"

  values: "{{values}}"
  labels: "{{labels}}"
  colors: "{{colors}}"
  vertical: "{{vertical}}"
  starting_location: "{{starting_location}}"
  interval_ratio_x: "{{interval_ratio_x}}"
  interval_ratio_y: "{{interval_ratio_y}}"
  legend: "{{legend}}"
  export_data: "{{export_df}}"
