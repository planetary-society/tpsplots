data:
  source: china_comparisons_controller.china_space_science_mission_mass_growth_line_chart

chart:
  type: line
  output: china_us_average_mass_comparison
  title: "{{title}}"
  subtitle: "{{subtitle}}"
  source: "{{source}}"

  x: "{{x}}"
  y: "{{y}}"
  label: "{{label}}"
  marker: "{{marker}}"
  markersize: "{{markersize}}"
  color: "{{color}}"
  tick_rotation: "{{tick_rotation}}"
  label_size: "{{label_size}}"
  tick_size: "{{tick_size}}"
  legend: "{{legend}}"
  ylabel: "{{ylabel}}"
  ylim: "{{ylim}}"
  export_data: "{{export_df}}"
