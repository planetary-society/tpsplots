data:
  source: nasa_fy2026_controller.directorates_comparison_grouped

chart:
  type: grouped_bar
  output: fy2026_directorate_cuts
  title: "NASA faced big cuts across the board in 2026"
  subtitle: "Only human exploration would have received a boost. All other programs would have been slashed or eliminated."
  source: "NASA FY 2026 Budget Request"

  categories: "{{categories}}"
  groups: "{{groups_pbr}}"
  scale: billions
  show_values: true
  value_format: monetary
  value_fontsize: 7
  tick_size: 10
  legend:
    loc: upper right
  export_data: "{{export_df}}"
