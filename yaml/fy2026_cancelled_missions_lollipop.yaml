data:
  source: fy2026_charts.cancelled_missions_lollipop_chart

chart:
  type: lollipop
  output: fy_2026_proposed_mission_cancellations
  title: "{{total_projects}} active science missions cancelled"
  subtitle: "These projects reflect more than {{total_value}} of investment by U.S. taxpayers and {{total_development_time}} of combined years to build."
  source: "FY 2026 White House NASA Request"

  categories: "{{categories}}"
  start_values: "{{start_values}}"
  end_values: "{{end_values}}"
  sort_by: start
  sort_ascending: false
  colors: "#037CC2"
  xlim: "{{xlim}}"
  start_value_labels: true
  xlabel: null
  grid: true
  y_axis_position: right
  hide_y_spine: true
  grid_axis: x
  tick_size: 12
  end_marker_style: X
  end_marker_color: red
  marker_size: 11
  value_labels: false
  range_labels: false
  category_wrap_length: 25
  export_data: "{{export_df}}"
